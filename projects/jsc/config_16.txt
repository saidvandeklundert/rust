system {
    host-name router-1;
    root-authentication {
        encrypted-password "$some_hash"; ## SECRET-DATA
    }
    protect: services {
        ftp;
        ssh;
        telnet;
        netconf {
            ssh;
        }
    }
}